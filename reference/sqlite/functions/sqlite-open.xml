<?xml version="1.0" encoding="iso-8859-1"?>
<!-- EN-Revision: 1.11 Maintainer: darvina Status: ready -->
<refentry id="function.sqlite-open">
 <refnamediv>
  <refname>sqlite_open</refname>
  <refpurpose>Apre un database SQLite e crea il database se non esiste.</refpurpose>
 </refnamediv>
 <refsect1>
  <title>Descrizione</title>
  <methodsynopsis>
   <type>resource</type><methodname>sqlite_open</methodname>
   <methodparam><type>string</type><parameter>filename</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>mode</parameter></methodparam>
   <methodparam choice="opt"><type>string</type><parameter>&amp;error_message</parameter></methodparam>
  </methodsynopsis>
  <para>
   Restituisce una risorsa (database handle) se ha successo, oppure &false; se si verifica un errore.
  </para>
  <para>
   Il parametro <parameter>filename</parameter> indica il nome del 
   database. Può essere un percorso relativo o assoluto al file che SQLite 
   utilizzerà per memorizzare i dati. Se il file non esiste, SQLite tenterà di 
   crearlo. <emphasis>BISOGNA</emphasis> avere i permessi
   di scrittura sul file se si desidera inserire dei dati o modficare 
   lo schema del database.
  </para>
  <para>
   Il parametro <parameter>mode</parameter> specifica la modalità del file ed è inteso
   per essere utilizzato per l'apertura del file in modalità read-only.
   Attualmente questo parametro viene ignorato da SQLite. Per default la
   modalità di apertura è <literal>0666</literal> e questo è il valore
   raccomandato se si necessita di accedere al parametro
   <parameter>errmessage</parameter>.
  </para>
  <para>
   Il parametro <parameter>errmessage</parameter> è passato per riferimento ed è impostato per 
   contenere un messaggio descrittivo di errore nel caso non si riesca ad
   aprire il database a causa di un errore.
  </para>
  <para>
   <example>
    <title>Esempio di uso di <function>sqlite_open</function></title>
    <programlisting role="php">
<![CDATA[
<?php
if ($db = sqlite_open('mysqlitedb', 0666, $sqliteerror)) { 
    sqlite_query($db, 'CREATE TABLE foo (bar varchar(10))');
    sqlite_query($db, "INSERT INTO foo VALUES ('fnord')");
    $result = sqlite_query($db, 'select bar from foo');
    var_dump(sqlite_fetch_array($result)); 
} else {
    die($sqliteerror);
}
?>]]>
    </programlisting>
   </example>
  </para>
  <tip>
   <simpara>
    Sui sistemi Unix, SQLite è sensibile agli script che utilizzano la chiamata di sistema fork(). 
    Se si sta utilizzando un tale script, si raccomanda di chiudere la connessione prima dell'esecuzione
    del fork, e quindi ri-aprirlo nel processo figlio/padre.
    Per maggiori informazioni su questo particolare vedere
    <ulink url="&url.sqlite.interface.c;">L'interfaccia C alla
    libreria SQLite</ulink> nella sezione intitolata
    <literal>Multi-Threading e SQLite</literal>. 
   </simpara>
  </tip>
  <tip>
   <simpara>
    Si raccomanda di non utilizzare database SQLite su partizioni montate via NFS.
    Poiché è noto che NFS non è affidabile con i lock,
    può capitare che si riesca neppure ad aprire il database, o, se ci si riesce,
    il comportamento dei lock può essere indefinito.
   </simpara>
  </tip>
  <note>
   <simpara>
    A partire dalla versione 2.8.2 della libreria SQLite, si può specificare
    <literal>:memory:</literal> come valore per il parametro <parameter>filename</parameter> in modo
    da creare database che risiedano soltanto in memoria.
    Principalmente questo è utile per le elaborazioni temporanee, poiché
    il database in memoria sarà distrutto non appena il processo termina. Può anche
    essere utilizzato in coppia con l'istruzione SQL <literal>ATTACH DATABASE</literal> 
    per caricare altri database in modo da potere muovere i dati tra di loro.
   </simpara>
  </note>
  <note>
   <simpara>
    SQLite sensibile al &safemode; e a open_basedir.
   </simpara>
  </note>

  <para>
   Vedere anche <function>sqlite_popen</function>,
   <function>sqlite_close</function> e
   <function>sqlite_query</function>.
  </para>
 </refsect1>
</refentry>
<!-- Keep this comment at the end of the file
 Local variables:
 mode: sgml
 sgml-omittag:t
 sgml-shorttag:t
 sgml-minimize-attributes:nil
 sgml-always-quote-attributes:t
 sgml-indent-step:1
 sgml-indent-data:t
 sgml-parent-document:nil
 sgml-default-dtd-file:"../../manual.ced"
 sgml-exposed-tags:nil
 sgml-local-catalogs:nil
 sgml-local-ecat-files:nil
 End:
 vim600: syn=xml fen fdm=syntax fdl=2 si
 vim: et tw=78 syn=sgml
 vi: ts=1 sw=1
 -->
