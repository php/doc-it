<?xml version="1.0" encoding="iso-8859-1"?>
<!-- EN-Revision: 1.15 Maintainer: geppo Status: ready -->
 <reference id="ref.mysql">
  <title>MySQL Functions</title>
  <titleabbrev>MySQL</titleabbrev>

  <partintro>
   <section id="mysql.intro">
    &reftitle.intro;
    <para>
     Queste funzioni consentono l'accesso ai server di database MySQL.
     Maggiori informazioni riguardo MySQL possono essere trovate su <ulink
     url="&url.mysql;">&url.mysql;</ulink>.
    </para>
    <para>
     La documentazione su MySQL può essere trovata su <ulink
     url="&url.mysql.docs;">&url.mysql.docs;</ulink>.
    </para>
   </section>

   <section id="mysql.requirements">
    &reftitle.required;
    <para>
     Al fine di rendere queste funzioni disponibili, si deve compilare PHP con
     il supporto MySQL.
    </para>
   </section>
    
    &reference.mysql.configure;

    &reference.mysql.ini;

   <section id="mysql.resources">
   &reftitle.resources;
    <para>
     Ci sono due tipi di risorsa usati nel modulo MySQL. Il primo
     è l'identificativo di connessione per una connessione ad un database, del secondo  tipo sono
	 le risorse che contengono i risultati di una query.
    </para>
   </section>

   &reference.mysql.constants;

    
   <section id="mysql.examples">
   &reftitle.examples;
    <para>
     Questo esempio mostra come connettersi, eseguire una query, stampare
     le righe risultanti e disconnettersi dal database MySQL.
     <example>
      <title>Esempio dell'estensione MySQL</title>
      <programlisting role="php">
<![CDATA[
<?php
    /* Connessione e selezione del database */
    $connessione = mysql_connect("host_mysql", "utente_mysql", "password_mysql")
        or die("Connessione non riuscita");
    print "Connesso con successo";
    mysql_select_db("mio_database") or die("Selezione del database non riuscita");

    /* Esecuzione di una query SQL */
    $query = "SELECT * FROM mia_tabella";
    $risultato = mysql_query($query) or die("Query fallita");

    /* Stampa dei risultati in HTML */
    print "<table>\n";
    while ($linea = mysql_fetch_array($risultato, MYSQL_ASSOC)) {
        print "\t<tr>\n";
        foreach ($linea as $valore_colonna) {
            print "\t\t<td>$valore_colonna</td>\n";
        }
        print "\t</tr>\n";
    }
    print "</table>\n";

    /* Liberazione delle risorse del risultato */
    mysql_free_result($risultato);

    /* Chiusura della connessione */
    mysql_close($connessione);
?>
]]>
      </programlisting>
     </example>
    </para>
   </section>
  </partintro>

&reference.mysql.functions;

 </reference>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

